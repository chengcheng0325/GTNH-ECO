# 02 · COVER — 覆盖板配置界面

- 主题 ID：`gregtech:cover`（GTGuiThemes.COVER，parent = STANDARD）
- 背景纹理：同 STANDARD（`singleblock_default.png`）

## 与 STANDARD 的差异（仅文字色）
- 正文文字 `#555555`、标题 `#222222`（更暗），输入框白色

## 风格一句话
STANDARD 的暗字变体，专用于覆盖板（cover）配置小界面。

## 适用
GT 覆盖板 GUI。